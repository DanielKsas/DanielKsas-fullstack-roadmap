/** 
 * Write a JavaScript program to get the difference between a given number and 13,
 * if the number is broader than 13 return double the absolute difference*/
    let numeroMayor = 13
    let numeroDado = -5
    let diferenciaNumero = numeroMayor -numeroDado
    if(numeroMayor > diferenciaNumero){
             diferenciaNumero = (numeroDado - numeroMayor) * 2
    } else{
      diferenciaNumero = (numeroDado - numeroMayor)*-1
    }

  